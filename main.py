# program to calculate the perimeter of a rectangle

length=int(input('enter the length'))
width=int(input('enter the width'))

rectangle = 2 * (length + width)
print(f'the perimeter of the rectangle is {rectangle} ')

